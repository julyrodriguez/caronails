// useWebPushNotifications.ts (Native version - does nothing to avoid importing firebase/messaging on mobile)
export function useWebPushNotifications(user: any) {
  // Web push notifications are not supported on native mobile platforms
}
